export const cookieParams = {
  accessToken: "accessToken",
  refreshToken: "refreshToken",
  isActive: "isActive", //Determines if a user is logged in or not
  isSeeker: "isSeeker", // Determines if the user is a seeker or not
  isProfileBuild: "isProfileBuild", // Determines if a user's profile has been build or not
  email: "email", // Store the user's email ID
  role: "role", //Store the role of the user 
};
