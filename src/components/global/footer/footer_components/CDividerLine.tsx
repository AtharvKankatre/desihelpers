import styles from "@/styles/Common.module.css";

export const CDividerLine = () => {
  return (
    <div className={styles.dividerLineWrapper}>
      <div className={styles.dividerLine} />
    </div>
  );
};
